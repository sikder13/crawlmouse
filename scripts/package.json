{
  "name": "@crawlmouse/scripts",
  "version": "0.0.1",
  "private": true,
  "type": "module",
  "scripts": {
    "smoke": "tsx smoke-crawl.ts",
    "test": "vitest run",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "@crawlmouse/engine": "workspace:*"
  },
  "devDependencies": {
    "tsx": "^4.19.0",
    "typescript": "^5.5.4",
    "vitest": "^2.0.5"
  }
}
