# DELTA GATE 10 — 3 of 4 hold. (a) FAILS on one line. No PR.

**Frozen SHA** `f6a54a5dc3396e60fdc4399aa9b33f69c2ec20ee` · baseline `b6e046f` · worktree `../cm-g10`
· one independent reviewer · Node v22.23.1.

| check | result |
|---|---|
| **(a)** every quoted number re-measured against the final tree, zero discrepancies | ❌ **FAIL — 1** |
| **(b)** the three gate-9 survivors die against the row-shape matrix | ✅ **PASS** |
| **(c)** `git diff b6e046f..HEAD` touches only tests, fixtures, docs | ✅ **PASS** |
| **(d)** full uncached suites, typecheck, lint, build | ✅ **PASS** |

**PR NOT OPENED.**

---

## (a) FAIL — one stale figure, and it is the one gate 9 named

`docs/tickets/2026-08-07-frontier-catalog-guard-uncovered-shapes.md:18`

> **Measured, all four applied at once on top of the real migrations: 0 discovered, 0 violations.**

| | |
|---|---|
| claimed | 0 discovered, 0 violations |
| **measured at this SHA** | **1 discovered, 4 violations** |

```
PROBE-ALLFOUR-HOSTILE-DISCOVERED ["reap_via_view"]
PROBE-ALLFOUR-VIOLATIONS         ["reap_via_view: SECURITY DEFINER — would run as the owner",
                                  "reap_via_view: PUBLIC holds privileges (=X/postgres)",
                                  "reap_via_view: anon holds privileges (anon=X/postgres)",
                                  "reap_via_view: authenticated holds privileges (authenticated=X/postgres)"]
```

**Why this one matters more than its size.** `evidence/2026-08-08-gate9-reports.md:55` filed FC-1 as
*three* strings: the CLOSED row, the title, **and this bolded headline**. The fix pass corrected the
row and the title and left the headline — so the artifact is internally contradictory: its own
evidence file prints `reap_via_view: DISCOVERED` and the guard now carries a pinned assertion that it
is discovered, while this sentence says nothing was.

It is also the ticket's load-bearing sentence: it is the stated evidence for the accepted risk.

**The cause is the same one §10's new rule names.** The correction targeted the paragraph that had
been *added* at gate 9 and never re-read the whole artifact against the final tree. Correcting what
you are looking at is not the same as re-measuring what ships.

## (b) PASS — all three survivors die at exactly the claimed counts

Harness proved live first: unconditional `throw` at `AuditView.tsx` module scope → `1 failed (1)`,
`Tests no tests`. Restores by `cp`; `git checkout --` never used.

| mutation | `AuditView.test.tsx` | four guards |
|---|---|---|
| `crawlHealth` nulled only when `confidence === 'low'` | **3 failed \| 15 passed (18)** | 91/91 |
| `snapshot={done ? snapshot : null}` | **1 failed \| 17 passed (18)** | 91/91 |
| delete `onActivity` from the `wireAuditStream` call | **1 failed \| 17 passed (18)** | 91/91 |

The other 11 sweep rows were re-run independently and **every one reproduces**.

## (c) PASS — zero production-source changes

```
git diff --name-only b6e046f..HEAD -- apps/web/app apps/web/lib apps/web/components \
  apps/web/middleware.ts 'packages/*/src' inngest infra \
  ':!*.test.ts' ':!*.test.tsx' ':!*__tests__*' ':!*__fixtures__*'
→ (no output)
```

9 files: 2 tests + 7 docs. No renames, deletions, mode changes or binaries.

## (d) PASS

engine **65/837** · web **208/1562** · inngest **8/145** · scripts **2/40**, all exit 0.
`turbo typecheck --force` 5/5 **0 cached** · `turbo lint --force` 4/4 **0 cached**, no warnings ·
`next build` ✓ 71/71 static pages, exit 0. Every §7 figure matches to the test.

---

## ★ THE MATERIAL FINDING — a FOURTH survivor, same family, larger blast radius

Not part of the four checks, and **not a shipped-behaviour defect** — the shipped code is correct.
Reported because it is the fourth consecutive gate whose surviving finding is in the fixtures.

```tsx
// apps/web/app/audit/[id]/AuditView.tsx:137 — replacing snapshot={snapshot}
snapshot={snapshot && (snapshot.page_count ?? 0) > 4 ? { ...snapshot, crawlHealth: null } : snapshot}
```

**Verified independently, on the whole tree:**

```
apps/web:  Test Files 208 passed (208)   Tests 1562 passed (1562)
tsc --noEmit: exit 0     next lint: ✔ No ESLint warnings or errors
```

Effect, demonstrated by replay against the real route:

```
refused row,   page_count 10 → "Couldn’t grade this site" + "usually a site that blocks crawlers",
                                NO_GRADE_LABEL absent          ← gate 3's blocker, verbatim
graded v2 row, page_count 10 → v2 arc marker absent            ← demoted to the legacy GradeCard
```

**Production reachability, re-measured independently:**

| | |
|---|---|
| audits that would refuse | **51** |
| …of those, `page_count > 4` → failure card instead of the Stage 4 arc | **12** |
| v2 completed audits | **206** |
| …of those, `page_count > 4` → lose the v2 arc (gap, free cure, conversion spine) | **167** |

**Why the matrix cannot see it.** The matrix varies `confidence`, `partial`, a zero-page row and an
activity ring. **Every fixture row still has `page_count` of 2 or 0** — and the same is true of
`cms_detected`, `settings.pageCap`, viewer identity, `link_count`, `estimateSource` (never
`'sitemap'`), and the trigger set (never `no_observed_links`, which is **36 of the 51** real refusals).
`page_count` is the cheapest member of a family, not a one-off.

**The pattern, stated plainly.** Each pass has varied the axes someone predicted, and each gate has
found an edit keyed on an axis nobody varied: JSX → props → stream shape → row shape → *row-shape
attributes*. Enumerating attributes is the same losing move as enumerating SQL spellings was at gates
4–6. What ended that class was changing the medium — a catalog assertion instead of a text matcher.
The analogous move here is to stop hand-authoring row shapes and derive them from real data (replay
against actual production row shapes, or property-generate rows over the payload's declared type and
assert the invariant "a refused audit never renders the failure card" across all of them), so that no
single attribute is a hiding place.

**OWNER RULING, 2026-08-08 — ACCEPT THE FIXTURE LIMIT EXPLICITLY AND SHIP.**

> "Three consecutive gates with zero shipped-behaviour defects and production source untouched since
> `347e6e8` — the bar is met. Fixture-evasion completeness was never the bar, and the attribute
> progression (JSX → props → stream → row shape → attributes) proves that enumeration cannot
> terminate. The doctrine applies to our own tests: state what is covered, claim nothing about what
> escapes."

So the limit is **stated**, not left to be discovered: `AuditView.test.tsx`'s header now names the axes
it varies (confidence, partial, page-emptiness, activity) and the axes it does not (`page_count`,
`cms_detected`, `pageCap`, viewer identity, `link_count`, `estimateSource`, the trigger set), with this
survivor and its reachability recorded there. The medium change is scheduled as the top 5.1b
test-infrastructure item — `docs/tickets/2026-08-08-fixture-medium.md` — with the single invariant
"a refused audit never renders the failure card" asserted across derived or generated rows, which is
the catalog-not-text-matcher move applied to fixtures.

---

## Incidental (reviewer, outside the four checks)

- `evidence/2026-08-08-gate9-fix-pass.md:242` — the "Output" block re-labels the query's columns
  (`proxy_too_few` vs the query's `proxy_too_few_true`). Values correct, labels not what the query
  emits, in the section whose purpose is re-derivability.
- `packages/engine/src/refusal.ts:20-21` cites §5 as *n=215*; §5's new box, added in the same commit,
  says the 2026-08-08 figure is **212**. A cross-reference re-measured on one side only.
- The sweep table's mutation text is not precise enough to re-derive first try (an additive card
  conditioned on `verdict === 'refused'` gives 9/18 + 3/91; unconditional gives 10/18 + 5/91).
  A mutation whose exact text is not committed cannot be re-measured by anyone else.
- §5's sign-off table still reads `215 / 51 (23.7%)` three lines below a box disclosing 212. Internally
  consistent, but two denominators in one section.
- Delta trace-audit clean: single author, no authorship trailer, no secret-shaped strings.

## Verdict

**(b), (c), (d) held on the reviewed SHA. (a) did not** — one line, corrected in the follow-up commit
after re-reading the WHOLE artifact against the final tree (the ticket's "four shapes" framing and its
Related list were stale too, and are fixed with it). Re-measured by the controller: **1 discovered,
4 violations**, matching the reviewer byte for byte.

The fourth survivor is **accepted as a stated limit** per the owner's ruling above, with FIXTURE-MEDIUM
filed. Shipped behaviour: zero blockers, three consecutive gates, unanimous.
